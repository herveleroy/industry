= form_tag( {:controller => 'caterpillars', :action => 'search'}, :method => 'get', :remote => true) do
  .row.collapse
    .nine.columns
      = text_field_tag :search, params[:search], :id => 'search_field'
    .three.columns
      = submit_tag "Rechercher", :name => nil, :class => "button expand postfix"
  = link_to_function "RAZ", "$('search_field').clear()"
%p Résultats
#search_results
  = form_tag add_caterpillars_idea_path, :method => :post, :remote => true do
    = hidden_field_tag("id", idea.id)
    %ul
    = submit_tag "Associer les chenilles sélectionnés", :class => "small success radius button"



