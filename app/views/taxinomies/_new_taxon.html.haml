= simple_form_for Tag.new , :remote => true do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.hidden_field :taxon, :value => "1"

    %br

  .form-actions
    = f.button :submit, :class => "small success radius button"


